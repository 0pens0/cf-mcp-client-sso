apiVersion: v1
kind: Service
metadata:
  name: cf-mcp-client-k8s
  namespace: demo
  labels:
    app.kubernetes.io/name: cf-mcp-client-k8s
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: cf-mcp-client-k8s
  ports:
    - name: http
      port: 80
      targetPort: http
